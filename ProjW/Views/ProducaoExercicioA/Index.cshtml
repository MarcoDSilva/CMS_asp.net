
@{
    ViewBag.Title = "Index";
}

<h2>Produção Exercicio A</h2>
<h5>Eliminar uma determinada linha de tarefa se ela não for a única existente</h5>

<div>
    <p>Qual o <strong>ID</strong> da linha de tarefa a eliminar?</p>

    @using (Html.BeginForm())
    {
        <div>
            @Html.TextBox("id_tarefa")
            <input type="submit" name="submit" value="Confirmar" />
        </div>

        if (ViewBag.linhas > 1)
        {
            <p>
                A <u>linha de tarefa</u> com o <strong>ID</strong> @ViewBag.idLinha corresponde à <u>tarefa</u> com o <strong>ID</strong>
                @ViewBag.tarefa e fazia parte de um total de @ViewBag.linhas <u>linhas de tarefa</u>.
                Foi procedido ao apagamento desse registo.
                A tarefa contém agora <strong>@ViewBag.linhasNovas</strong> <u>linha(s) de tarefa</u>.
            </p>
        }
        else if (ViewBag.linhas == 1)
        {
            <p>
                A <u>linha de tarefa</u> com o <strong>ID</strong> @ViewBag.idLinha corresponde à <u>tarefa</u> com o <strong>ID</strong>
                @ViewBag.tarefa e é a unica <u>linha de tarefa</u>.
                Sendo assim, o registo <strong>NÃO</strong> pode ser apagado.
            </p>
        }
        else
        {
            <p>@ViewBag.aviso</p>
        }


    }


</div>
